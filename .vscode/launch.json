{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Check Site",
            "type": "chrome",
            "request": "launch",
            "url": "http://localhost:8080",
            "webRoot": "${workspaceFolder}"
        }
    ]
}